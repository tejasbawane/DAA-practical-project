#include < limits.h>
#include ‹ stdio.h›
#define MAX 20
int V,E;
int graph [MAX][MAX];

# d e fi n e INFINITY 99999
void dijkstra(int G[MAX][MAX], int n,int startnode) {

int cost[MAX][MAX], dist[MAX], pred[MAX];
int visited[MAX], count, min_dist, nxtnode;
f o r (int i=1; i<=n; i++){
for (int j=1; j<=n; j++){
i f ( G [ i ] l j ] = 0 && i !=j){
c o s t [ i ] [ j ] = INFINITY;
Jelset
cost[i][j] = G[i][j];
}
}
for (int i=1; i<=n; i++){
dist[i] = cost[startnode][i];
pred[i] = startnode;
v i s i t e d [ 1 ] = 0 ;
}
dist[startnode] = 0;
v i s i t e d [ s t a r t n o d e ] count = 1;
= 1;
while (count ‹ n-1)
{
min_dist = INFINITY;
for (int i=1; i<=n; i++){
if (dist[i] ‹ min_dist & !visited[i]){
min_dist = d i s t [ i ] ;
nxtnode = i;
}
visited[nxtnode] = 1;
for (int i=1; i<=n; i++){
if (!visited[i] & min_dist + cost[nxtnode][i] < dist[i]){
= min_dist + cost[nxtnode][i];
pred[i] = nxtnode;
}
}count++;
}
printf("Node\tDistance\tPath\n");
for(int i=1; i<=n; i++){
if (i != startnode){
printf("%4d\t", i);
if(dist[i] == INFINITY){
printf(" INF\ENO PATH\n");
Jelsef
printf("%8d\t", dist[i]);
printf("%d", i);
int j=i;
dof
j=pred[j];
printf("<-%d", j) ;


}while(j!=startnode);
printf("\n");
}
}
int main() {
int s , d , w, i , j ;
printf("Enter the number of vertices : ");
scanf (*%d", &V);
p r i n t f " E n t e r the number of e d g e s : ");
scanf ("%d", &E) ;
for (i = 1 ; i <=V; i++) {
for (j=1; j <= V; j+t ) {
graph [i][1] = 0;
}
}
for (i=1;i<=E;i++) {
printf("Enter source : ");
scanf ("%d",&s);
printf("Enter destination : ");
scanf ("%d", &d);
printf("Enter weight : ");
scanf("%d", &w);
if(s > V I| d › V || s<=0 || d<=0) {
printf("Invalid index. Try again.\n");
i - - ;
c o n t i n u e ;
} e l s e {
g r a p h [s][d] = w;

}
}
printf("Enter the source :");
scanf("%d", &s);
dijkstra(graph, V,s);
return 0;
}
}
